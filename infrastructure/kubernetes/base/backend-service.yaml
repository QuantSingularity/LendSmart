apiVersion: v1
kind: Service
metadata:
  name: lendsmart-backend
  namespace: default
  labels:
    app: lendsmart-backend
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: lendsmart-backend
