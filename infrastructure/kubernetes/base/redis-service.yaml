apiVersion: v1
kind: Service
metadata:
  name: lendsmart-redis
  namespace: default
  labels:
    app: lendsmart-redis
    component: cache
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app: lendsmart-redis
